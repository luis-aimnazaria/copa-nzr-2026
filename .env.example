# Copie para .env.local (dev) ou configure nas variáveis de ambiente da Vercel.
# Sem estas variáveis o app roda em modo demonstração (LocalStorage).
# Valores em: Supabase → Project Settings → API
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anon-aqui
